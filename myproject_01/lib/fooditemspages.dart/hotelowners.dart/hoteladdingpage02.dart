import 'package:flutter/material.dart';

class Hoteladdingpage02 extends StatelessWidget {
  const Hoteladdingpage02({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: Text('add food items oi9ip',style: TextStyle(color: Colors.black),),),
    );
  }
}